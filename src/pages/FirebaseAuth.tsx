import FirebaseAuthPanel from "../components/firebase/FirebaseAuthPanel";

export default function FirebaseAuth() {
    return (
        <section className="flex min-h-[70vh] items-center justify-center px-4">
            <FirebaseAuthPanel />
        </section>
    );
}